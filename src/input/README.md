# Input

This module handles input processing for the game, including mapping input actions to game state changes and dispatching input events to the appropriate handlers based on the current game state.